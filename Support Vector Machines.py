# Import libraries
from sklearn.svm import SVC
# define data, split to train & test, create model and fit data
X = Variables
Y = Classes
model = SVC(kernel =, params).fit(X, Y)
# Predict new classes
Y_predict = Model.Predict(X_test)
# Score model
accuracy_score(Y_test, Y_predict)
